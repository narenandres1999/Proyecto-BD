/*
const db = require ("../function.collections/exportar");
const express = require ("express");
const router = express.Router();
router.use(express.json());
router.use(express.urlencoded({extended:true}))

router.route("/")
.get((req,res)=>{
})
router.route ("/:ruta")
.get(db.getAll)

module.exports = router;
*/